import { ClearCompletedPipe } from './clear-completed.pipe';

describe('ClearCompletedPipe', () => {
  it('create an instance', () => {
    const pipe = new ClearCompletedPipe();
    expect(pipe).toBeTruthy();
  });
});
